rockchip-vaapi (1.0.8-1) noble; urgency=medium

  * Fix H.264 decode: mpp_init must be called before MPP_DEC_SET_CFG and
    MPP_SET_OUTPUT_BLOCK (mpp_init resets all prior configuration); previously
    every H.264 frame timed out in SyncSurface because non-blocking mode was
    never applied.
  * Fix VP9 altref temporal glitch: invalidate prime_fd when a hidden
    (show_frame=0) altref frame is decoded. MPP never outputs altref frames via
    decode_get_frame, so the surface retains the fd from its previous use as a
    P-frame. If Firefox later issues show_existing_frame pointing to that surface,
    ExportSurfaceHandle now returns VA_STATUS_ERROR_INVALID_SURFACE instead of
    serving stale content, eliminating frames-from-the-past appearing mixed with
    current frames.
  * SyncSurface: return VA_STATUS_ERROR_DECODING_ERROR on 3s drain timeout
    instead of SUCCESS, so callers can distinguish a properly decoded surface
    from one that was never assigned an MPP frame.
  * do_h264_decode / do_generic_decode: mark render_target as decoded
    immediately when no VASliceDataBufferType is found (BeginPicture+EndPicture
    without slice data, e.g. after a crash recovery sequence).
  * do_generic_decode: scale EndPicture poll window with packet size
    (min 500ms, max 3000ms) so large 4K VP9 keyframes do not time out.
  * Add src/va_test.c: synthetic VA-API decode correctness test for VP9.

 -- Eduardo García-Mádico Portabella <woodyst@gmail.com>  Wed, 28 May 2026 04:00:00 +0000

rockchip-vaapi (1.0.7-1) noble; urgency=high

  * Fix NS_ERROR_DOM_MEDIA_FATAL_ERR crash at quality transitions: BeginPicture
    no longer closes the surface's prime_fd. Firefox calls ExportSurfaceHandle
    from a compositor thread without SyncSurface while EndPicture polls MPP;
    prime_fd was -1 causing ERROR_INVALID_SURFACE. Now BeginPicture keeps the
    previous fd (placeholder or last decoded frame) valid; assign_mpp_frame
    replaces it atomically when the real decoded frame arrives.
  * assign_mpp_frame: update s->width/s->height from actual decoded frame
    dimensions so ExportSurfaceHandle reports the correct stride after a DASH
    quality switch (mismatch between surface width and pitch was also causing
    NS_ERROR_DOM_MEDIA_FATAL_ERR in earlier sessions).
  * do_h264_decode / do_generic_decode: add pre-drain loop before
    decode_put_packet to consume frames MPP already has queued from previous
    packets; reduce EndPicture poll window from 500ms to 100ms.
  * SyncSurface: extend drain timeout from 2s to 3s.

 -- Eduardo García-Mádico Portabella <woodyst@gmail.com>  Wed, 28 May 2026 01:00:00 +0000

rockchip-vaapi (1.0.6-1) noble; urgency=high

  * Fix startup freeze: SyncSurface now actively drains MPP instead of
    blocking on a pthread cond that can never be signalled (no decode thread).
    Stores ctx_id back-reference in RKSurface; SyncSurface calls
    decode_get_frame in a loop until the surface is ready or 2s timeout.
  * Extract assign_mpp_frame() helper shared by EndPicture and SyncSurface,
    eliminating duplicated frame routing/assignment code.

 -- Eduardo García-Mádico Portabella <woodyst@gmail.com>  Tue, 27 May 2026 20:30:00 +0000

rockchip-vaapi (1.0.5-1) noble; urgency=medium

  * Fix video freeze (image static, audio continues) caused by three bugs:
    - do_h264_decode: broke after first MPP output frame without checking if
      it was the render_target; with H.264 B-frames this caused 2s timeouts.
      Now drains until render_target.decoded, mirroring do_generic_decode.
    - do_generic_decode FIFO: advancing dq_head past non-head PTS matches
      orphaned earlier surfaces, causing 2s SyncSurface timeouts per frame.
      Now only advances head when matched surface is at the front.
    - CreateContext: MPP blocking decode_get_frame stalled Firefox's GPU
      thread causing full process hangs. Now sets MPP_SET_OUTPUT_BLOCK=0.

 -- Eduardo García-Mádico Portabella <woodyst@gmail.com>  Tue, 27 May 2026 20:10:00 +0000

rockchip-vaapi (1.0.4-1) noble; urgency=low

  * Add MOZ_ENABLE_WAYLAND=1 to environment: Firefox uses Wayland natively
    when WAYLAND_DISPLAY is set (e.g. sway), enabling full DMABUF zero-copy.
  * Add MOZ_X11_EGL=1 to environment: Firefox uses EGL on X11 sessions,
    enabling hardware WebRender via Mali-G610 (panthor/panfrost, GLES 3.1).

 -- Eduardo García-Mádico Portabella <woodyst@gmail.com>  Fri, 25 Apr 2026 14:30:00 +0000

rockchip-vaapi (1.0.3-1) noble; urgency=low

  * Add debconf question to configure system-wide VA-API environment variables.
  * Install /etc/profile.d/rockchip-vaapi.sh (terminal sessions).
  * Install /etc/environment.d/60-rockchip-vaapi.conf (systemd desktop sessions).
  * Supports dpkg-reconfigure to change the choice after installation.

 -- Eduardo García-Mádico Portabella <woodyst@gmail.com>  Thu, 24 Apr 2026 15:00:00 +0000

rockchip-vaapi (1.0.2-1) noble; urgency=low

  * Fix author name to full name: Eduardo García-Mádico Portabella, EGP Sistemas.
  * Add README.md, INSTALL.md and docs/DEVELOPMENT.md.

 -- Eduardo García-Mádico Portabella <woodyst@gmail.com>  Thu, 24 Apr 2026 14:00:00 +0000

rockchip-vaapi (1.0.1-1) noble; urgency=low

  * Change license from MIT to LGPL-2.1-or-later.
  * Add LGPL-2.1+ SPDX headers to all source files.

 -- Eduardo García-Mádico Portabella <woodyst@gmail.com>  Thu, 24 Apr 2026 13:00:00 +0000

rockchip-vaapi (1.0.0-1) noble; urgency=low

  * Initial release.
  * VA-API 1.20 vtable implementation bridging libva to librockchip-mpp1.
  * H.264 hardware decode with DRM PRIME 2 zero-copy surface export.
  * H.264 Annex B SPS/PPS reconstruction via Exp-Golomb encoding.

 -- Eduardo García-Mádico Portabella <woodyst@gmail.com>  Thu, 24 Apr 2026 12:00:00 +0000
